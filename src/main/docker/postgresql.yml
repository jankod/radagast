version: '2'
services:
  radagast-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/Radagast/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=Radagast
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
